import { NavbarThemeDirective } from './navbar-theme.directive';

describe('NavbarThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarThemeDirective();
    expect(directive).toBeTruthy();
  });
});
